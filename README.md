# Assets

Reserved for screenshots or a recorded terminal session (e.g. an
[asciinema](https://asciinema.org) cast) demonstrating the CLI in use.

Suggested content to capture here before publishing the repo:

- A terminal recording running through `load`, `shortest-path`, `mst`,
  and `route` against `examples/network.txt`
- A screenshot of `make test` passing with the full 37-test summary

Once added, link them from the "Screenshots" section of the root
`README.md`.
